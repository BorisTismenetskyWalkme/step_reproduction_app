services:
  - type: web
    name: step-reproduction-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python step_reproduction_app/app.py
    envVars:
      - key: AUTOMATION_BS_USER
        sync: false
      - key: AUTOMATION_BS_PASS
        sync: false